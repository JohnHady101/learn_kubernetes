# This is a kind configuration file for creating a Kubernetes cluster with one control-plane node and two worker nodes.
# run it with the command: kind create cluster --config cluster.yaml


kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane
  - role: worker
  - role: worker